
"pasta_projeto": "É a pasta raiz aonde está o projeto backend",
"front": "É a pasta raiz aonde está o projeto front end",
"versao": "1.0",
"modelagem": "é a modelagem em sí"

"nome": "Nome da tabela,controller etc. !!Singular e começa com letra maiuscula!!"
"render": " 'sim' para gerar || 'não' para não gerar "
"estrutura": "São os atributos e propriedades"


"nome": "Minusculo, e separedo por underline se for composto"
"label": "É o nome, apelido"
"tipo": "varchar||integer||decimal||datetime||attach"
"subtipo": "email||password||name||varchar||  phone||boolean||cpf||cpnj||integer||amount|| money||decimal|| datetime|| datetime||"
"join":  ["11|1N", "Nome da table", "Nome do atributo que vai ser destaque", "atributo que é a foregnkey, por default é mesmo da estrutura", "nome da chave customizada",]



